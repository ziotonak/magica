// TEST: Field access through pointer

Point :: struct { x: i32, y: i32 }

foreign exit :: (code: i32)

main :: () -> i32 {
    p := Point.{ x = 0, y = 0 }

    ptr := ^p
    ptr.x = 5

    if ptr.x != 5 {
        exit(1)
    }

    return 0
}
