// TEST: Field access through pointer used as expression

Point :: struct { x: i32, y: i32 }

foreign exit :: (code: i32)

main :: () -> i32 {
    p := Point.{ x = 10, y = 20 }

    ptr := ^p
    v := ptr.x

    if v != 10 {
        exit(1)
    }

    return 0
}
